server:
  port: 8383

spring:
  datasource:
    url: jdbc:mysql://172.31.29.63:3306/shopping?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: shopuser
    password: Shop@1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

management:
  endpoints:
    web:
      exposure:
        include:
          - "*"
  server:
    port: 8081
